import { Logger } from '../../../zego.entity';
import { ConfigState } from '../../common';
export declare class ZipLogUploader {
    private _configState;
    private _innerLogger;
    private proxyCtrl?;
    private _defaultDomain;
    private _uploadUrl;
    private _uploadURLs;
    private _uploadIndex;
    private _taskQueue;
    private _updateLocked;
    constructor(_configState: ConfigState, _innerLogger: Logger, proxyCtrl?: any);
    private _isUploadFieldReady;
    setUploadUrl(urls: string[]): void;
    sendRequest(formData: FormData): Promise<void>;
    upload(zipBlob: Blob): Promise<void>;
}
